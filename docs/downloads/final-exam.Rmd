---
title: "Final Exam"
author: "Pablo E. Gutiérrez-Fonseca"
date: "`r format(Sys.time(), '%Y-%b-%d, %I:%M %p')`"
output:
  html_document:
    df_print: paged
subtitle: ''
editor_options: 
  markdown: 
    wrap: sentence
---

<br><br> 

### General Instructions / Instrucciones Generales

- This is the Midterm-Exam. Each question is worth 20 points.
Este es el examen parcial. Cada pregunta vale 20 puntos.
<br>

- You must choose 5 out of the 6 questions to answer. **Your maximum score is therefore 100 points.**<br>
Debe escoger 5 de las 6 preguntas para contestar.
Su puntaje máximo será 100 puntos.
<br>

- It is highly recommended that all your results, including tables and outputs, are always visible in your R Markdown document.
Es altamente recomendado que todos los resultados, incluidas las tablas y salidas, sean siempre visibles en su documento de R Markdown.
<br>

- The exam must be completed and submitted during class time, on **Tuesday, December 16, 2025, from 3:00 to 5:00 PM.**
El examen se debe realizar y entregar durante el tiempo de clase, es decir, Martes 16 de diciembre, de 3 a 5 pm.
<br>

- Use R and the tidyverse packages to answer all questions.
Utilice R y los paquetes tidyverse para contestar todas las preguntas.
<br>

- Work independently and make sure your code is reproducible.
Trabaje de manera independiente y asegúrese de que su código sea reproducible.
<br><br><br>

```{r message=F, warning=FALSE}
library(ggplot2)
library(dplyr)
library(palmerpenguins)
```
<br>

```{r, echo=FALSE, fig.cap="", out.width = '60%'}
#| fig-align: center
knitr::include_graphics("lter_penguins.png")
```


```{r, echo=TRUE, results='hide', message=FALSE}
penguins <- penguins
```
<br>

```{r, include=T}
head(penguins)
```
<br><br><br>

1.  Create a scatterplot using **geom_point()** that shows the relationship between flipper_length_mm and body_mass_g using the penguins dataset.<br> Crea un gráfico de dispersión usando geom_point() que muestre la relación entre bill_length_mm y bill_depth_mm usando el dataset penguins.<br>

-   Hint: Remember: **data = penguins**, **x = flipper_length_mm**, and **y = body_mass_g**. The geometry is **geom_point()**. The connector is **+**.
-   Sugerencia: Recuerda: data = penguins, x = flipper_length_mm, y = body_mass_g La geometría es geom_point(). El conector es +.

```{r warning=FALSE, include=F}
ggplot(data = penguins, 
    aes(
      x = flipper_length_mm, 
      y = body_mass_g 
      )) + 
geom_point()
```
<br><br>

2.  **Starting from the previous plot and using similar code, modify the scatterplot to color the points by species.**<br> Partiendo del gráfico anterior y usando un código similar, modifica el gráfico de dispersión para colorear los puntos por species.<br>

-   **Hint:** To map color to a variable, include color = species inside aes().
-   **Sugerencia:** Para asignar color a una variable, incluye color = species dentro de aes().

```{r warning=FALSE, include=F}
ggplot(data = penguins,
       aes(x = flipper_length_mm,
           y = body_mass_g,
           color = species)) +
  geom_point()
```
<br><br>


3.  **Starting from the previous plot and using similar code, modify the scatterplot to facet the data by species.**<br> Partiendo del gráfico anterior y usando un código similar, modifica el gráfico de dispersión para crear paneles separados por species.<br>
- **Hint:** Use facet_wrap(\~ species) to create one panel per species.
- **Sugerencia:** Usa facet_wrap(\~ species) para crear un panel por especie.
```{r warning=FALSE, include=F}
ggplot(data = penguins,
       aes(x = flipper_length_mm,
           y = body_mass_g)) +
  geom_point() +
  facet_wrap(~ species)
```
<br><br>

4. **Starting from the previous plot and using similar code, add a smooth line for each species.**<br>
Partiendo del gráfico anterior y usando un código similar, añade una línea de suavizado para cada especie.<br>
- **Hint:** Use geom_smooth() and remember that color = species must be inside aes() so each species gets its own line. You can use method = "lm".
- **Sugerencia:** Usa geom_smooth() y recuerda que color = species debe ir dentro de aes() para que cada especie tenga su propia línea. Puedes usar method = "lm".
```{r warning=FALSE, include=FALSE}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm,
                y = body_mass_g,
                color = species)
) +
  geom_point() +
  geom_smooth(method = "lm")
```
<br><br>

5. **Starting from the plot you created in Question 1, modify it to make it “publication-ready” by improving at least three aspects.**<br>
Partiendo del gráfico que creaste en la Pregunta 1, modifícalo para que sea apto para publicación mejorando al menos tres aspectos.<br>
- **Hint:** You can consider adding a title, adjusting axis text size, modifying axis labels, changing point size, or adding a legend.
- **Sugerencia:** Puedes agregar un título, cambiar el tamaño de los ejes, modificar los nombres de los ejes, cambiar el tamaño de los puntos o añadir una leyenda.
```{r warning=FALSE, include=FALSE}
ggplot(data = penguins,
       aes(x = flipper_length_mm,
           y = body_mass_g)) +
  geom_point(size = 3, alpha = 0.7) +           # ajustar tamaño y transparencia
  labs(title = "Relationship between Flipper Length and Body Mass",
       x = "Flipper Length (mm)",
       y = "Body Mass (g)") +
  theme_bw() +
  theme(axis.title = element_text(size = 14),
        axis.text = element_text(size = 12),
        legend.position = "bottom")
```
<br><br>

6. **Using the provided scatterplot and linear model summary, examine the relationship between flipper_length_mm and body_mass_g in penguins.**<br>
Usando el gráfico de dispersión y el resumen del modelo lineal provistos, examina la relación entre flipper_length_mm y body_mass_g en penguins.<br>

```{r warning=FALSE, echo=F}
ggplot(
  data = penguins,
  mapping = aes(x = flipper_length_mm, y = body_mass_g)
) +
  geom_point(mapping = aes(color = species)) +
  geom_smooth(method = "lm")
```

#### **Linea model**
```{r warning=FALSE, include=TRUE}
mod <- lm(body_mass_g ~ flipper_length_mm, data =
            penguins)
summary(mod)
```
<br><br>

**Questions / Preguntas (4 pts each / 4 pts cada una)**<br>
- **Important:**<br>
    - Provide only the numerical values exactly as they appear in the model output (including decimals or scientific notation).
Importante:
    - Provee únicamente los valores numéricos exactamente como aparecen en el output del modelo (incluyendo decimales o notación exponencial).
<br><br>

1. What is the R-squared value of the model?<br>
¿Cuál es el valor de R-cuadrado del modelo?<br>
<br>
2. What is the p-value for the slope coefficient?<br>
¿Cuál es el p-valor del coeficiente de la pendiente?<br>
<br>
3. Based on the p-value, is there a relationship between flipper length and body mass?<br>
Basado en el p-valor, ¿hay una relación entre longitud de aletas y masa corporal?<br>
<br>
4. What is the intercept?<br>
¿Cuál es el intercepto?<br>
<br>
5. What is the slope?<br>
¿Cuál es la pendiente?<br>


<br><br><br>


